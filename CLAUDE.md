@AGENTS.md

## Claude Code

This file exists only so Claude Code loads the shared project instructions. Do not duplicate project rules here.

Claude-specific reminders:

```text
Use /memory to verify which CLAUDE.md files are loaded when behavior seems inconsistent.
For subagents, explicitly tell each subagent to read AGENTS.md and the relevant docs before working.
Do not let multiple subagents edit the same file or API contract at the same time.
Subagent conclusions do not replace local confirmation from code, tests, Schema, OpenAPI, and docs.
```
