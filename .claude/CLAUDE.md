# CLAUDE.md：Smart_outdoor Claude Code Notes

Source of truth: `../AGENTS.md` and root `../CLAUDE.md`.

This file is only a Claude-specific note. Do not duplicate project rules here.

Claude Code should load the root `CLAUDE.md`, which imports `AGENTS.md`.

If Claude behavior seems inconsistent:

```text
1. Run /memory and confirm which CLAUDE.md files are loaded.
2. Re-read AGENTS.md.
3. Re-read docs/INDEX.md and the task-specific docs listed in AGENTS.md.
```
